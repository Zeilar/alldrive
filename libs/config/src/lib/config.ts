export const API_GLOBAL_PREFIX = "api";

/**
 * 5GB in bytes.
 */
export const MAX_DB_SIZE = 1000 * 1000 * 1000 * 5;

/**
 * 30 minutes in milliseconds.
 */
export const MAX_EXPIRATION = 1000 * 60 * 30;

export const EXTERNAL_ID_LENGTH = 5;
